from setuptools import setup, find_packages

setup(
    name='microsoft geo project',
    version='0.1',
    long_description='microsoft geo project',
    packages=find_packages(),
    install_requires=[],
)